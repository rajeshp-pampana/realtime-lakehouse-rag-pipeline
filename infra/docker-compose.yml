# Local development stack.
#
# Milestone 1 adds:  airflow (official image, slim single-scheduler + standalone), postgres
# Milestone 2 adds:  kafka (KRaft mode, single broker), tick-producer, stream-consumer
# Milestone 4 adds:  api (FastAPI)
# Milestone 5 adds:  ui (Streamlit), full Dockerfile wiring
# Milestone 6 adds:  prometheus, grafana
#
# On an 8 GB machine these are brought up in subsets (see README "Running locally").
# CI (.github/workflows/ci.yml) brings the full stack up as the end-to-end proof.

name: realtime-lakehouse-rag-pipeline

services:
  placeholder:
    image: hello-world
    profiles: ["never"]
